<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-M">
  <title>{{ titulo }}</title> <link rel="stylesheet" href="/assets/style.css"> </head>
<body>
  <h1>{{ titulo }}</h1>
  
  <img src="{{ image }}" alt="{{ titulo }}"> 
  
  <div>
    {{ content | safe }} 
  </div>

  <a href="/">Volver al inicio</a>
</body>
</html>